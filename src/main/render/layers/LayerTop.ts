import { ColonyCraft } from "../../ColonyCraft";
import { ScreenLayer } from "../ScreenLayer";

export class LayerTop extends ScreenLayer {
    constructor(game: ColonyCraft) {
        super(game, 100);
    }
}